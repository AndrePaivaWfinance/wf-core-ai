// MESH Platform - Modular Architecture
const { createMeshBot } = require("./bots/mesh");
const { configPromise } = require("./config/env");
const logger = require("./utils/logger");
console.log("🚀 MESH Platform (Modular) Starting...");
