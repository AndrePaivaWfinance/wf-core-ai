{
  "name": "mesh-platform",
  "version": "2.0.0",
  "dependencies": {
    "applicationinsights": "^2.8.1",
    "botbuilder": "^4.20.0",
    "dotenv": "^16.3.1",
    "restify": "^11.1.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.45.0",
    "jest": "^29.6.0",
    "prettier": "^3.0.0",
    "supertest": "^6.3.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "NODE_ENV=development node src/index.js",
    "prod": "NODE_ENV=production node src/index.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write src/",
    "docker:build": "docker build -t mesh-platform .",
    "docker:run": "docker run -p 3978:3978 mesh-platform"
  }
}
